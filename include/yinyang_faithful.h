#include <tuple>

struct yinyang_faithful {

    std::tuple<float*,size_t*,double> cluster(T* v, size_t n, size_t d, size_t k, T* centers, size_t* asg, size_t max_iter, double epsilon) {
        return;
    }
};